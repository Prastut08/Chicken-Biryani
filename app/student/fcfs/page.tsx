import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function StudentFCFSPage() {
  return (
    <PlaceholderPage
      title="FCFS"
      description="Course selection and allocation portal."
      breadcrumbs={[{ label: "Student" }, { label: "FCFS" }]}
    />
  );
}
