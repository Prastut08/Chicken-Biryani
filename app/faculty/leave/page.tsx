import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function FacultyLeavePage() {
  return (
    <PlaceholderPage
      title="Leave"
      description="Submit and track leave requests."
      breadcrumbs={[{ label: "Faculty" }, { label: "Leave" }]}
    />
  );
}
