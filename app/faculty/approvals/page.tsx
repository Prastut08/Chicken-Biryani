import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function FacultyApprovalsPage() {
  return (
    <PlaceholderPage
      title="Approvals"
      description="Review and process student course selection requests."
      breadcrumbs={[{ label: "Faculty" }, { label: "Approvals" }]}
    />
  );
}
