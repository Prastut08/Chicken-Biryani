import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function FacultyCalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      description="Academic calendar, events, and important dates."
      breadcrumbs={[{ label: "Faculty" }, { label: "Calendar" }]}
    />
  );
}
