import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function AdminPaymentsPage() {
  return (
    <PlaceholderPage
      title="Payments"
      description="Manage payments, fees, and transactions."
      breadcrumbs={[{ label: "Admin" }, { label: "Payments" }]}
    />
  );
}
