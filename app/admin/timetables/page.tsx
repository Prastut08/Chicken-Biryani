import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function AdminTimetablesPage() {
  return (
    <PlaceholderPage
      title="Timetables"
      description="Manage timetable entries and schedules."
      breadcrumbs={[{ label: "Admin" }, { label: "Timetables" }]}
    />
  );
}
