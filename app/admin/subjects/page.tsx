import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function AdminSubjectsPage() {
  return (
    <PlaceholderPage
      title="Subjects"
      description="Manage subjects, codes, credits, and categories."
      breadcrumbs={[{ label: "Admin" }, { label: "Subjects" }]}
    />
  );
}
